<%= simple_form_for([restaurant, review]) do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <div class="text-right">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  <% end %>